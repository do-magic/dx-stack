# Add files here to ignore them from prettier formatting
/dist
/coverage
/.nx/cache
/.nx/workspace-data
.nx/self-healing

# lockfiles are machine-generated; reformatting them is pure diff noise
pnpm-lock.yaml

# vendored, tool-synced skill docs (identical copies across .agents/.github/.opencode)
.agents/skills
.github/skills
.opencode/skills